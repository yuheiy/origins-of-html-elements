# The origins of HTML elements

このリポジトリは、現行HTML要素がどの文書体系、ソフトウェア、メディア、UI慣習、または標準化活動から生まれたかを、史料に基づいて追跡するWikiである。現在の意味から起源を推測せず、確認できない接続は未解決のまま残す。

このファイルをWikiの構造、調査、執筆、保守に関する唯一の作業規約とする。[llm-wiki.md](llm-wiki.md)は設計背景を説明する非規範的資料であり、ファイル名や運用が異なる場合はこのファイルを優先する。閲覧用の入口と要素一覧は[wiki/README.md](wiki/README.md)に置く。

## リポジトリの構成

- `raw/`: 繰り返し参照する一次資料の無加工スナップショット。収録内容は[raw/README.md](raw/README.md)に記録する。
- `wiki/README.md`: Wikiの入口、収録対象、抽出規則、境界事例、進捗、全要素への正本索引。
- `wiki/elements/`: 要素ごとの現在の調査結果。原則1要素につき1ファイルとし、共通史料と系譜を持つ`h1`〜`h6`は`hx.md`へまとめる。
- `wiki/lineages/`: 複数の現行要素が共有する上流、要求、標準化経路、要素間変更と、それらに共通する制約を扱う。網羅的な全関係一覧ではなく、共通祖先からの採用、複数要素を一体として導入した要求、または現行要素間の直接変更を一次資料で確認できる代表的な系統だけを収録する。同じ仕様に共存するだけの要素、単一要素だけで完結する系譜、機能上の対比だけの関係は収録しない。名称、用途、外見の類似や、未確認の改名、再採録、統合を矢印で補わない。個別要素の初出、HTMLへの導入、現在の意味、確度、証拠表、未解決点は各要素ページを正本とし、共有系譜では再掲しない。
- `wiki/log.md`: ingest、research、update、lintなどのappend-onlyな更新記録。調査、変更、検証の経緯はここだけに置く。

## 対象集合の更新

現在の対象集合、抽出規則、境界事例、最終確認日は[wiki/README.mdの「収録範囲」](wiki/README.md#収録範囲)を正本とする。

Living Standardの変更を確認したときは抽出規則を再検証し、`wiki/README.md`の収録範囲、最終確認日、件数、索引、進捗と`wiki/log.md`を同じ変更で更新する。

## 証拠の基準

### 史料の評価

史料は次の順に探索する。ただし、資料種別の順位よりも、対象とする主張への直接性、同時代性、来歴の明確さを優先する。

1. 当時の仕様、提案、DTD、標準化議事録、メーリングリスト、実装資料、ソースコード。
2. 当事者による同時代または回顧的な説明。
3. 標準化団体や保存機関による史料解説。
4. 学術研究および出典を追跡できる二次資料。
5. 一般的な解説記事。

下位の資料は探索の入口には使えるが、到達可能な一次資料がある主張の最終根拠にはしない。アーカイブの複製を使う場合は、元資料の作成者、日付、表題、保存元を記録する。

史料が矛盾するときは、都合のよい一方だけを採用しない。双方の主張、作成時期、立場、一次資料への距離を記録し、解消できない差異は`未解決`とする。後代の回顧が同時代資料と食い違う場合は、回顧を当事者の認識を示す証拠として扱い、過去の事実を単独で上書きする証拠にはしない。

### 主張の扱い

- 歴史的な各主張には、それを直接支える出典を同じ段落または証拠表の同じ行に置く。
- 因果関係を主張する場合は、当時の資料が採用、派生、改名、設計上の影響、または解決しようとした要求を示していなければならない。
- 名称や外見の類似、現在の用途、印刷物の同概念、GUIの同形ウィジェット、後代の使用例は探索仮説にはなるが、単独では由来の証拠にしない。
- 一つの参照を、その資料が述べていない複数の主張へ流用しない。
- 引用は判断に必要な短い範囲にとどめ、必ず前後関係を確認する。
- 証拠表には資料の日付、資料名、種別、確認できる内容、URL、閲覧日を記す。日付を推定した場合はその旨を明記する。

### 記述の区分

- **証拠**: 出典が明示する内容、または史料上直接観察できる内容。
- **解釈**: 複数の証拠から編集者が導く説明。証拠と同じ文へ圧縮しない。
- **出発仮説**: ハンドオフや既存研究から得たが、このWikiでは史料確認を終えていない説明。
- **否定された仮説**: 反証された説明、または由来として示すには証拠不足と判定した説明。
- **未解決**: 調査しても確定できない点。

出発仮説は結論や系譜へ確定事項として載せない。検証後に証拠、解釈、否定された仮説、または未解決へ移す。

## 確度と進捗

確度は、現在の意味や調査量ではなく、HTMLへの導入とその直前の系譜について得られた証拠を評価する。

| 等級 | 判定基準 |
|---|---|
| A | 要素単位の具体的な先行技術、要素、実装、または慣習からHTMLへ入った因果関係を直接史料で確認できる。 |
| A− | 因果関係は直接史料で確認できるが、集合単位の記述しかない、個別対応が不明、または中間経路の一部が欠ける。確認できる区間と欠落を明記する。 |
| B | HTMLへの導入理由または設計モデルは確認できるが、具体的な先行物から当該要素への因果関係は確認できない。 |
| C | 確認可能な最初期の出現と当時の意味は特定できるが、導入理由と具体的祖先は確認できない。 |
| 未評価 | 調査範囲が不足しており、AからCのいずれかを判定できない。 |

個別ページには一つの総合確度を付ける。これはHTMLへの導入と直前の系譜を評価する値である。さらに上流に複数の枝がある場合は枝ごとの証拠水準を本文で説明してよいが、総合確度や進捗へ重複計上しない。`hx.md`の総合確度は、同じ評価を共有する`h1`〜`h6`の6要素へそれぞれ計上する。

`wiki/README.md`の進捗は次の規則で数える。

- **史料検証済み**: `## 確度`がA、A−、B、Cのいずれかである要素数。由来が証明済みという意味ではなく、記録した調査範囲に基づく評価が済んだことを表す。
- **史料未検証**: `## 確度`が`未評価`である要素数。
- **現在の意味を要約済み**: `## 現在の意味`にLiving Standard本文の要約、一次定義へのリンク、確認日があり、プレースホルダーが残っていない要素数。
- **敵対的検証済み**: 下記検証に合格し、frontmatterの`status`が`完成`で、`wiki/log.md`に合格記録がある要素数。

frontmatterの`status`は`調査中`、`要検証`、`完成`のいずれかとし、日付や作業経緯を含めない。確認できた範囲と主要な制約は`概要`に記す。

## 要素ページの契約

ファイルは`wiki/elements/{要素名}.md`に置き、先頭のYAML frontmatterに`status`を置く。タイトルは ``# `<element>` `` とする。`h1`〜`h6`だけは`wiki/elements/hx.md`へまとめ、タイトルを ``# `<h1>`〜`<h6>` `` とする。続く見出しは次の順序を保つ。

| 見出し | 必須内容 |
|---|---|
| `## 概要` | 要素、判明した系譜、主要な未解決点の短い要約。未調査ならその事実。 |
| `## 現在の意味` | Living Standard上の定義の要約、一次定義へのリンク、確認日。ここから歴史を推定しない。 |
| `## HTMLへの導入` | 初出、導入主体、導入理由を分離した記述。不明な項目は推定で埋めない。 |
| `## HTML直前の祖先` | HTMLへの採用との因果関係を確認できる先行技術、要素、実装、提案、または慣習。なければ`未確認`。 |
| `## さらに上流の由来` | `### 証拠`に史料が直接示す背景、`### 解釈`に証拠から導く説明。成立する解釈がなければ`なし`。 |
| `## 系譜` | 確認済みの関係だけを結んだ系譜。集合レベルの関係や欠落は注記し、未確認の接続は矢印で補わない。 |
| `## 証拠` | 下記の列を持ち、各行を本文中の主張へ対応させた表。 |
| `## 確度` | A、A−、B、C、または`未評価`と、その判定理由。 |
| `## 否定された仮説` | 撤回、反証、保留した説明と理由。なければ`なし`。 |
| `## 未解決` | 系譜上の欠落と対応する、追加調査で答えるべき具体的な問い。 |

証拠表の列は次のとおりとする。

| 年月日 | 資料 | 種別 | この資料から確認できる内容 | URL | 閲覧日 |
|---|---|---|---|---|---|

プレースホルダーを機械的に埋めず、各節が担う問いへ最短で明確に答える。調査や変更の経緯ではなく、現在の結論を直接記す。複数要素が共有する関係は`wiki/lineages/`へ統合するが、個別ページにもその要素の結論を直接支える証拠を残す。

## 調査と敵対的検証

### 調査

1. `wiki/README.md`と関連する`wiki/lineages/`を読み、対象、既知の仮説、重複調査を確認する。
2. Living Standardで現在の定義を確認する。
3. HTML上の初出候補から以前の仕様、実装、DTD、議論へ遡り、導入主体、理由、直接祖先を探す。
4. 直接祖先が参照する、さらに古い文書体系、ソフトウェア、メディア、UI慣習を調べる。
5. 証拠、解釈、仮説を分離し、各主張を証拠表へ対応させる。
6. 競合する説明と撤回した説明を`否定された仮説`、系譜上の欠落を`未解決`へ反映し、確度を判定する。成果がなかった検索や作業経緯は正本文書へ記録しない。
7. 関連する個別ページ、共有系譜、`wiki/README.md`の進捗、`wiki/log.md`を同じ変更で更新する。

### 敵対的検証

敵対的検証は初回調査と分離した読み直しとして実施する。同じ編集者が実施してもよいが、結論を支持する探索の再実行ではなく、反証、より早い出現、別系統、引用の文脈欠落を積極的に探す。

次のすべてを満たしたときだけ合格とする。

1. 各歴史的主張を出典本文へ戻って確認でき、因果関係を述べる主張には因果関係そのものの証拠がある。
2. 日付、主体、要素名、引用の文脈を取り違えていない。
3. より早い版、競合資料、反例を探索し、結論へ影響する内容を本文、`否定された仮説`、`未解決`へ反映している。
4. 系譜の各矢印、確度、未解決点が証拠表と整合する。
5. 必須見出し、相対リンク、進捗集計に不整合がない。

合格時はfrontmatterの`status`を`完成`とし、`wiki/log.md`へ日付、対象、結果を記録する。別個の検証報告書は作らない。歴史的結論、証拠、系譜、確度を後から変更した場合は`status`を`要検証`として再検証する。現在の定義の確認日だけを更新した場合は、歴史部分の再検証を必須としない。

見出しが揃っただけでは完成としない。由来を確定できない場合も、確認可能な最初期の出現、直接の証拠、未解決点が記録され、上記検証を通過すれば正式な成果とする。

## Wikiの保守

- `wiki/README.md`は閲覧者向けの正本索引とし、要素ページの追加、削除、改名、進捗変更時に更新する。
- `wiki/log.md`には`## [YYYY-MM-DD] 操作 | 対象`形式で追記する。過去の記録本文は書き換えず、訂正は新しい項目から参照する。正本文書の移動や削除に伴うリンク先だけは更新してよい。
- 横断的な系譜や集計へ反映するのは、個別ページで史料検証済みの関係だけとする。ハンドオフの結論や確度は出発仮説として扱う。
- 新しい資料が既存の主張を強化、限定、否定するときは、関連する全要素ページと共有系譜の整合を取る。
- 相対リンク、必須見出し、空の証拠表、汎用プレースホルダー、進捗集計のずれを定期的に検査する。

## Rawリソース

`raw/`には、Wikiで繰り返し参照する一次資料、対象集合を支える資料、複数要素へ証拠が波及する重要資料だけを保存する。一度しか参照しない出典を網羅的に複製しない。

外部の正式URLを正本とし、ローカルファイルは改訂や消失に備えた検証用コピーとする。HTMLおよびテキストの応答本文は整形・変換せず保存する。ZIP、tarなどの配布アーカイブは展開し、アーカイブ自体は保存しない。展開時は配布物内のファイル内容とディレクトリ構造を変更しない。一つの仕様または版を`raw/`直下の一ディレクトリへまとめ、異なる版で保存済み資料を上書きしない。

追加と再取得は[`tools/fetch-raw.sh`](tools/fetch-raw.sh)を経由し、取得元と保存先を[`tools/raw-sources.tsv`](tools/raw-sources.tsv)へ記録する。正式な配布元または信頼できる保存元から取得し、意図した文書であることを確認する。アーカイブは展開前に内部一覧を検査し、展開後に収録ファイルを確認する。仕様、版、保存ファイルを変更した場合は、取得日、正本URL、目的、Living Standardの収録規則を[raw/README.md](raw/README.md)で同時更新する。
