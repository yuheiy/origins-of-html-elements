# The origins of HTML elements

このリポジトリは、現行HTML要素がどの文書体系、ソフトウェア、メディア、UI慣習、標準化活動から生まれたかを、史料に基づいて追跡するLLM Wikiである。現在の意味から起源を推測せず、確認できない接続は未解決のまま残す。

このファイルをWikiの唯一のスキーマとする。[llm-wiki.md](llm-wiki.md)は非規範的な設計背景であり、実際の構造と操作にはこのスキーマを適用する。

## アーキテクチャ

### Rawソース

`raw/`は、Wikiの主張を遡って検証するための不変かつ信頼できる唯一の情報源である。人間は調査対象を定めて重要な判断を行い、LLMはその範囲内で資料を探索、選定、追加する。保存済みの内容は変更、上書き、削除しない。訂正または削除には、人間の明示的な承認を必要とする。

Wikiで根拠に使う資料は、現行仕様を含めて`raw/`へ保存する。外部URLは来歴の記録と公開版へのアクセスのために保持するが、検証対象として固定するのはローカルのスナップショットである。

### Wiki

`wiki/`はLLMが生成し、一貫性を維持する永続的なWikiである。人間は内容を読み、問いと判断を与える。LLMはページの作成、統合、相互参照、訂正、およびREADMEとログの管理を担う。

- `wiki/README.md`: Wikiの概要、収録範囲、全知識ページへの案内、および人間向けの概要。
- `wiki/raw-index.md`: Rawに保存された全ソースファイルの生成索引。
- `wiki/elements/`: 現行HTML要素ごとの結論。
- `wiki/lineages/`: 複数の現行要素が共有する確認済みの祖先、要求、標準化経路、または現行要素間の変更。
- `wiki/syntheses/`: 比較、横断分析、再利用可能な統合結果。
- `wiki/log.md`: 操作単位の追記専用ログ。

### スキーマ

`AGENTS.md`は、構造、知識モデル、ページ規約、操作、完了条件を定義する唯一のスキーマである。スキーマは人間から明示的な指示があった場合にのみ変更し、`schema`操作としてログへ記録する。

## 知識モデル

### 証拠

史料は次の順に探索する。ただし、資料種別の順位より、主張への直接性、同時代性、来歴の明確さを優先する。

1. 当時の仕様、提案、DTD、議事録、メーリングリスト、実装資料、ソースコード。
2. 当事者による同時代または回顧的な説明。
3. 標準化団体や保存機関による史料解説。
4. 学術研究と出典を追跡できる二次資料。
5. 一般的な解説記事。

下位の資料は探索の入口に使えるが、到達可能な一次資料がある主張の最終根拠にはしない。複製を使う場合は元資料の作成者、日付、表題、保存元を記録する。

史料が矛盾するときは双方の主張、作成時期、立場、一次資料への距離を記録し、解消できない差異を`未解決`とする。後代の回顧は当事者の認識を示す証拠として扱い、同時代資料を単独で上書きする証拠にはしない。

### 主張

- **証拠**: 出典が明示する内容、または史料上直接観察できる内容。
- **解釈**: 複数の証拠から編集者が導く説明。証拠と同じ文へ圧縮しない。
- **出発仮説**: ハンドオフや既存研究から得たが、このWikiでは史料確認を終えていない説明。
- **否定された仮説**: 反証された説明、または由来として示すには証拠不足と判定した説明。
- **未解決**: 調査しても確定できない、追加調査で答えるべき具体的な問い。

歴史的な各主張には、それを直接支える出典を同じ段落または証拠表の同じ行に置く。資料名をRawへの相対リンクとし、その直後に`（[公開元](外部URL)）`を併記する。一つの参照を資料が述べていない主張へ流用せず、引用は判断に必要な短い範囲にとどめて前後関係を確認する。

因果関係は、当時の資料が採用、派生、改名、設計上の影響、または解決しようとした要求を示す場合だけ記す。名称、用途、外見の類似、現在の用途、後代の使用例は出発仮説にはなるが、単独で由来の証拠にはしない。未確認の接続を系譜の矢印で補わない。

出発仮説は結論や系譜へ確定事項として載せず、検証後に証拠、解釈、否定された仮説、または未解決へ移す。

### 確度と状態

確度は要素ページにだけ一つ置き、HTMLへの導入とその直前の系譜を評価する。調査量や現在の意味は評価しない。

| 等級 | 判定基準 |
|---|---|
| A | 要素単位の具体的な先行技術、要素、実装、慣習からHTMLへ入った因果関係を直接史料で確認できる。 |
| A− | 因果関係は直接確認できるが、集合単位の記述しかない、個別対応が不明、または中間経路の一部が欠ける。 |
| B | HTMLへの導入理由または設計モデルは確認できるが、具体的な先行物から当該要素への因果関係は確認できない。 |
| C | 確認可能な最初期の出現と当時の意味は特定できるが、導入理由と具体的祖先は確認できない。 |
| 未評価 | 調査範囲が不足し、AからCを判定できない。 |

全知識ページのフロントマターに`status: 調査中|要検証|完成`を置く。`完成`は、ページ規約、知識検証、対象リントに合格した状態を表す。歴史的結論、証拠、系譜、確度を変更したページは`要検証`とし、敵対的検証を終えた後にだけ`完成`へ戻す。現行定義の確認日だけを変更した場合、歴史部分の再検証は不要である。

`h1-h6.md`は、6要素の導入経路、確度、状態が共通する限り一ページで扱い、同じ値を各要素へ計上する。差異を示す証拠が得られた場合は`status: 要検証`とし、人間の明示的な指示によってスキーマが変更されるまで`完成`にしない。

## ページ規約

### README

`wiki/README.md`はWikiの入口と対象要素一覧の正本であり、Wikiの目的と読み方、Living Standardからの抽出規則、境界事例、確認日、Raw索引、すべての要素ページ、共有系譜ページ、統合ページへのリンク、および人間向けの一行概要を記録する。概要は、一覧だけで確認済みの由来または経路を識別できる長さにする。解釈を左右する主要な未解決点だけを添え、各ページの`概要`は全文再掲しない。ページの追加、削除、改名、または主要な結論の変更によって概要が不正確になる場合は、対応するREADME項目も同じ変更で更新する。ログは運用記録として案内するが、各項目は要約しない。

### Raw索引

`wiki/raw-index.md`はフロントマターを持たない生成索引であり、生成物であることを本文に明記する。`raw/`直下の取得元ホスト別ディレクトリに保存された全ファイルを、Rawからの相対パスの昇順で、リンクと文書タイトルとともに列挙する。タイトルは、HTML文書と判定できるファイルの先頭256 KiB以内にある`title`要素から抽出し、確認できない場合は`—`とする。Rawを追加、削除、移動した場合は、`scripts/generate-raw-index.js`で再生成する。

### 要素ページ

ファイルは`wiki/elements/{要素名}.md`に置き、タイトルを``# `<要素名>` ``とする。`h1`から`h6`だけは`h1-h6.md`へまとめ、タイトルを``# `<h1>`〜`<h6>` ``とする。

次の見出しをこの順序で置く。

| 見出し | 必須内容 |
|---|---|
| `## 概要` | 判明した系譜と主要な未解決点の短い要約。 |
| `## 現在の意味` | Living Standard上の定義の要約、資料名からRawへのリンク、公開一次定義への`公開版`リンク、確認日。 |
| `## HTMLへの導入` | 初出、導入主体、導入理由を分離した記述。不明な項目は推定しない。 |
| `## HTML直前の祖先` | HTMLへの採用との因果関係を確認できる先行物。なければ`未確認`。 |
| `## さらに上流の由来` | `### 証拠`に史料が直接示す背景、`### 解釈`に証拠から導く説明。成立しなければ`なし`。 |
| `## 系譜` | 確認済みの関係だけを結んだ系譜。集合レベルの関係と欠落を注記する。 |
| `## 証拠` | 本文の歴史的主張へ対応する証拠表。 |
| `## 確度` | A、A−、B、C、未評価のいずれかと判定理由。 |
| `## 否定された仮説` | 撤回、反証、保留した説明と理由。なければ`なし`。 |
| `## 未解決` | 系譜上の欠落に対応する具体的な問い。なければ`なし`。 |

証拠表は次の列を持つ。

| 年月日 | 資料 | 種別 | この資料から確認できる内容 | 閲覧日 |
|---|---|---|---|---|

`資料`欄では資料名をRawへリンクし、その直後に`（[公開元](外部URL)）`を置く。複数の資料を一行で扱う場合は、各保存資料を識別できるリンク名と`公開元1`、`公開元2`を対応させる。同じ配布物から保存した複数ファイルは、一つの公開元を共有してよい。

各節には、調査経緯ではなく現在の結論を直接記す。複数要素が共有する関係を共有系譜ページへ統合しても、要素ページにはその要素の結論を直接支える証拠を残す。

### 共有系譜ページ

共有系譜ページは、共通祖先からの採用、複数要素を一体として導入した要求、または現行要素間の直接的な変更を一次資料で確認できる場合にのみ作る。同じ仕様に共存するだけの要素、単一要素だけで完結する系譜、機能上の対比にとどまる関係は収録しない。個別要素の初出、現在の意味、確度、証拠表、未解決点は要素ページを正本とする。見出し順は固定しない。

### 統合ページ

統合ページは、比較、横断分析、クエリーから得た再利用価値のある統合結果を扱う。証拠と解釈を分け、粒度ごとの正本へリンクする。見出し順は固定しない。

同じ知識を複数ページへ全文再掲しない。要素ページは要素単位の結論、共有系譜ページは複数要素間の確認済みの関係、統合ページは横断的な解釈の正本とする。

## 操作

Wikiを書き換える操作では、影響する全ページと、ページ規約が求める場合はREADMEも同じ変更で整合させる。検証後にログへ一項目を追記して、操作を完了する。LLMは指定された調査範囲内で資料と探索経路を選ぶ。対象範囲、解釈、強調点を変える重要な選択についてのみ、更新前に人間へ判断を求める。

### インジェスト

1. 与えられた資料を識別し、未収録のURLは`scripts/add-raw.js`で新しいRawスナップショットとして保存する。
2. Wiki内を検索して関連ページと既存Rawを読み、重複、補強、限定、矛盾、新しい接続を特定する。
3. 資料の要点を証拠、解釈、出発仮説へ分け、影響するページへ統合する。
4. 競合する説明を両方残し、結論、否定された仮説、未解決、状態を更新する。
5. Raw索引を再生成し、対象リントを実行して、`ingest`のログを一項目追記する。

### クエリー

1. Wiki内を検索して関連ページを選び、蓄積された結論を読む。
2. 必要な主張が未記録、競合、古い、または高い正確性を要する場合だけRawへ戻る。
3. WikiページとRawを引用して回答する。
4. 新しい主張、訂正、接続、再利用価値のある統合結果を得た場合にのみWikiへ書き戻し、対象リントを実行して`query`のログを記録する。

既存知識の言い換えや一度きりの回答はWikiへ保存せず、ログにも記録しない。

### 調査

1. Wiki内を検索し、関連する要素ページ、共有系譜ページ、統合ページ、未解決点を読んで、問いと既知の仮説を定める。
2. Living Standardで現在の定義を確認する。
3. HTML上の初出候補から以前の仕様、実装、DTD、議論へ遡り、導入主体、理由、直接祖先を探す。
4. 直接祖先が参照する上流の文書体系、ソフトウェア、メディア、UI慣習を調べる。
5. 使用する新資料をRawへ追加し、証拠、解釈、仮説を分離してWikiへ統合する。
6. 初回探索と分離した敵対的検証で、反証、より早い出現、競合資料、別系統、引用の文脈欠落を探す。
7. 結論、系譜、確度、否定された仮説、未解決、状態を確定し、対象リントを実行して`research`のログを記録する。

成果が得られなかった検索や作業経緯は正本ページへ残さない。調査中に追加した資料は、同じ操作内でインジェストの品質条件も満たす。別のログ項目には分けない。

### リント

対象リントでは、変更したページ、その直接の参照先と参照元、README、Raw索引、集計を検査する。全体リントを明示された場合は、全ページ、孤立ページ、矛盾の可能性、欠落した横断ページ、Raw参照、ログの整合性も検査する。

必須項目、見出し順、フロントマター、空の証拠表、汎用プレースホルダー、相対リンク、Raw参照、Raw索引の再現性とファイル網羅性、READMEのページ網羅性と概要の正確性、重複タイトル、状態、確度集計を確認する。各項目を`合格|修正済み|Research必要`のいずれかとしてログに要約し、未分類の問題または安全に修正できる不合格が残る場合はリント未完了とする。結論を変えない機械的な不整合は修正する。歴史的主張、系譜、確度、対象範囲に影響する問題は修正せず、`Research必要`として報告する。

明示的なリント操作では、対象、検査結果、修正内容、調査が必要な問題を`lint`のログ一項目へ記録する。

### スキーマ

スキーマの変更は人間の明示的な指示に基づき、変更理由、文脈、トレードオフ、必要な移行を`schema`のログ一項目へ記録する。スキーマ変更用のスキルは置かない。

`.agents/skills/ingest`、`.agents/skills/query`、`.agents/skills/research`、`.agents/skills/lint`は、対応する基本操作へのショートカットであり、このスキーマだけを参照する。

## ログ

`wiki/log.md`の本文は追記専用とし、過去の記録は書き換えない。ただし、正本文書の移動や削除に伴うリンク先の機械的な変更は例外とする。一つのユーザー起点の操作につき、一項目を記録する。

```text
## [YYYY-MM-DD] ingest|query|research|lint|schema | 対象
```

本文には統合した内容、変更した結論、検証結果、主要な制約を記す。過去の複数項目形式は履歴として維持する。

## 検証

敵対的検証は初回調査と分離し、結論を支持する探索の反復ではなく、反証を探す読み直しとして実施する。次のすべてを満たした場合だけ合格とする。

1. 各歴史的主張が知識モデルの証拠要件を満たす。
2. 日付、主体、要素名、引用の文脈を取り違えていない。
3. より早い版、競合資料、反例を探索し、結果を結論、否定された仮説、未解決へ反映している。
4. 系譜の各矢印、確度、未解決点が証拠表と整合する。
5. 対象リントが完了している。

由来を確定できない場合も、確認可能な最初期の出現、直接の証拠、未解決点を記録して検証を通過すれば正式な成果とする。

## Rawソースの保守

Rawスナップショットは追加後に変更しない。同じURLの内容が変わった場合、別のスナップショットには公式のバージョン付きURLまたは信頼できるアーカイブURLを使う。誤収録を訂正または削除する場合は人間の明示的な承認を得て、新しいログ項目で過去の記録を訂正する。

取得した資料は`raw/{取得元ホスト}/{URLパス}`へ置き、取得元のディレクトリ構造を維持する。末尾が`/`のURLは`index.html`とする。クエリー付きURLには、衝突を避けるため、URLパスの末尾に`-`と取得元URLのSHA-256先頭16桁を接尾辞として付ける（例：`file.cgi-{hash}`）。ファイルシステムで安全に扱えないパス文字はパーセントエンコードし、連続する`/`は一つに正規化する。ファイルは導出したパスへ保存する。アーカイブは同じパスをディレクトリとし、内部のディレクトリ構造を変えずに展開する。保存先は取得元URLから決定論的に導出する。ディレクトリ配置は、資料間の関係、採用の因果関係、証拠上の優劣を意味しない。

大規模な資料群は、Wikiが参照する資料と、対象要素の集合または境界の検証に必要な資料だけを保存してよい。実際の収録範囲は、WikiからのRaw参照と`wiki/raw-index.md`で確認する。

HTMLおよびテキスト形式の応答本文は、変換せずに保存する。配布アーカイブは展開前に内容一覧を検査する。アーカイブ自体は保存せず、展開後の内容とディレクトリ構造をGitで管理する。

URLからの取得には`scripts/add-raw.js`を使い、正式な配布元または信頼できる保存元にある意図した文書であることを確認する。取得処理は一時領域で完了させてから新しい保存先へ追加し、失敗した場合は既存のRawを変更しない。取得元URL、リダイレクト後の最終URL、取得日を該当操作のログへ記録する。Rawを変更した操作では、`scripts/generate-raw-index.js`を使って索引を再生成する。
