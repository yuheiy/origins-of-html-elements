# The origins of HTML elements

このリポジトリは、現行HTML要素がどの文書体系、ソフトウェア、メディア、UI慣習、標準化活動から生まれたかを、史料に基づいて追跡するLLM Wikiである。現在の意味から起源を推測せず、確認できない接続は未解決のまま残す。

このファイルをWikiの唯一のスキーマとする。[llm-wiki.md](llm-wiki.md)は非規範的な設計背景であり、実際の構造と操作にはこのスキーマを適用する。スキーマは人間から明示的な指示があった場合にのみ変更し、理由、文脈、トレードオフ、必要な移行を`schema`操作としてログへ記録する。

## アーキテクチャ

### Rawソース

`raw/`は、Wikiの主張を遡って検証するための不変かつ信頼できる唯一の情報源である。人間は調査対象を定めて重要な判断を行い、LLMはその範囲内で資料を探索、選定、追加する。保存済みの内容は変更、上書き、削除しない。訂正または削除には、人間の明示的な承認を必要とする。

Wikiで根拠に使う資料は、現行仕様を含めて`raw/`へ保存する。外部URLは来歴の記録と公開版へのアクセスのために保持するが、検証対象として固定するのはローカルのスナップショットである。

### Wiki

`wiki/`はLLMが生成し、一貫性を維持する永続的なWikiである。

- `wiki/README.md`: Wikiの概要、収録範囲、全知識ページへの案内、および人間向けの概要。
- `wiki/raw-source-index.md`: Rawに保存された全ソースファイルの生成索引。
- `wiki/elements/`: 現行HTML要素ごとの結論。
- `wiki/lineages/`: 複数の現行要素が共有する確認済みの祖先、要求、標準化経路、または現行要素間の変更。
- `wiki/overviews/`: 要素ページと共有系譜を横断する分類、比較、調査状況の見取り図。
- `wiki/log.md`: 操作単位の追記専用ログ。

## 知識モデル

### 証拠

史料は次の順に探索する。ただし、資料種別の順位より、主張への直接性、同時代性、来歴の明確さを優先する。

1. 当時の仕様、提案、DTD、議事録、メーリングリスト、実装資料、ソースコード。
2. 当事者による同時代または回顧的な説明。
3. 標準化団体や保存機関による史料解説。
4. 学術研究と出典を追跡できる二次資料。
5. 一般的な解説記事。

下位の資料は探索の入口に使えるが、到達可能な一次資料がある主張の最終根拠にはしない。複製を使う場合は元資料の作成者、日付、表題、保存元を記録する。

史料が矛盾するときは双方の主張、作成時期、立場、一次資料への距離を記録し、解消できない差異を`未解決`とする。後代の回顧は当事者の認識を示す証拠として扱い、同時代資料を単独で上書きする証拠にはしない。

### 主張

- **証拠**: 出典が明示する内容、または史料上直接観察できる内容。
- **解釈**: 複数の証拠から編集者が導く説明。証拠と同じ文へ圧縮しない。
- **出発仮説**: ハンドオフや既存研究から得たが、このWikiでは史料確認を終えていない説明。
- **否定された仮説**: 反証された説明、または由来として示すには証拠不足と判定した説明。
- **未解決**: 調査しても確定できない、追加調査で答えるべき具体的な問い。

因果関係は、当時の資料が採用、派生、改名、設計上の影響、または解決しようとした要求を示す場合だけ記す。名称、用途、外見の類似、現在の用途、後代の使用例は出発仮説にはなるが、単独で由来の証拠にはしない。未確認の接続を系譜の矢印で補わない。

出発仮説は結論や系譜へ確定事項として載せず、検証後に証拠、解釈、否定された仮説、または未解決へ移す。

### 出典

歴史的な各主張には、それを直接支える出典を同じ段落または証拠表の同じ行に置く。資料名をRawへの相対リンクとし、その直後に`（[公開元](外部URL)）`を併記する。一つの参照を資料が述べていない主張へ流用せず、引用は判断に必要な短い範囲にとどめて前後関係を確認する。複数の資料を一つの証拠表行で扱う場合は、各保存資料を識別できるリンク名と`公開元1`、`公開元2`を対応させる。同じ配布物から保存した複数ファイルは、一つの公開元を共有してよい。

### 導入確度、HTML外への到達度、状態

導入確度は要素ページにだけ一つ置き、HTMLへの導入とその直前の系譜を評価する。先行物がHTML内か外か、HTML外をどこまで調べたか、現在の意味は評価しない。

| 等級 | 判定基準 |
|---|---|
| A | 要素単位の具体的な先行技術、要素、実装、慣習からHTMLへ入った因果関係を直接史料で確認できる。 |
| A− | 因果関係は直接確認できるが、集合単位の記述しかない、個別対応が不明、または中間経路の一部が欠ける。 |
| B | HTMLへの導入理由または設計モデルは確認できるが、具体的な先行物から当該要素への因果関係は確認できない。 |
| C | 確認可能な最初期の出現と当時の意味は特定できるが、導入理由と具体的祖先は確認できない。 |
| 未評価 | 調査範囲が不足し、AからCを判定できない。 |

HTML外への到達度は要素ページにだけ一つ置き、HTML標準化活動の外にある文書体系、ソフトウェア、メディア、UI慣習、または要求まで因果関係を遡れたかを、導入確度とは独立に評価する。これは優劣を表す等級ではなく、調査結果の分類である。

| 分類 | 判定基準 |
|---|---|
| 外部系譜確認済み | 特定のHTML外の先行物から要素への採用、派生、改名、または設計上の影響を直接史料で確認できる。 |
| 外部要求確認済み | 特定の外部祖先は確認できないが、HTML外の用途、問題、慣習、または利用者要求に応じて設計されたことを同時代資料で確認できる。 |
| 外部候補あり | 特定の外部候補または背景は確認できるが、要素への因果関係は確認できない。 |
| 外部未確認 | 調査した範囲ではHTML標準化内部の経路までしか確認できず、特定の外部候補も因果関係のある外部要求も確認できない。 |
| 未評価 | HTML外を対象とする調査範囲が不足している。 |

全知識ページのフロントマターに`status: 調査中|要検証|完成`を置く。`完成`は、ページ規約、知識検証、対象リントに合格した状態を表す。要素ページはHTML外への到達度が`未評価`の間は`完成`にしない。`外部候補あり`または`外部未確認`でも、探索範囲を満たし、再開可能な具体的な問いを`未解決`に残せば`完成`にできる。歴史的結論、証拠、系譜、導入確度、HTML外への到達度を変更したページは`要検証`とし、敵対的検証を終えた後にだけ`完成`へ戻す。現行定義の確認日だけを変更した場合、歴史部分の再検証は不要である。

`h1-h6.md`は、6要素の導入経路、導入確度、HTML外への到達度、状態が共通する限り一ページで扱い、同じ値を各要素へ計上する。差異を示す証拠が得られた場合は`status: 要検証`とし、人間の明示的な指示によってスキーマが変更されるまで`完成`にしない。

## ページ規約

### 共通

Wiki本文は、英語との不自然な混在や直訳調を避け、文脈に即した自然な日本語で記述する。原資料の表題、引用、仕様名、固有名詞、識別子、コードは原表記を保持する。

### README

`wiki/README.md`はWikiの入口と対象要素一覧の正本であり、Wikiの目的と読み方、Living Standardからの抽出規則、境界事例、確認日、Raw索引、すべての要素ページ、共有系譜ページ、概観ページへのリンク、および人間向けの一行概要を記録する。概要は、一覧だけで確認済みのHTML外の由来またはHTMLへの経路を識別できる長さにする。HTML外の由来が未確認であることが解釈を左右する場合はその旨と主要な未解決点を添え、各ページの`概要`は全文再掲しない。ページの追加、削除、改名、または主要な結論の変更によって概要が不正確になる場合は、対応するREADME項目も同じ変更で更新する。ログは運用記録として案内するが、各項目は要約しない。

### Raw索引

`wiki/raw-source-index.md`はフロントマターを持たない生成索引であり、生成物であることを本文に明記する。Rawを追加、削除、移動した場合は、`tools/generate-raw-source-index.js`で再生成する。

### 要素ページ

ファイルは`wiki/elements/{要素名}.md`に置き、タイトルを``# `<要素名>` ``とする。`h1`から`h6`だけは`h1-h6.md`へまとめ、タイトルを``# `<h1>`〜`<h6>` ``とする。

次の見出しをこの順序で置く。

| 見出し | 必須内容 |
|---|---|
| `## 概要` | 判明した系譜と主要な未解決点の短い要約。 |
| `## 現在の意味` | Living Standard上の定義の要約、資料名からRawへのリンク、公開一次定義への`公開版`リンク、確認日。 |
| `## HTMLへの導入` | 初出、導入主体、導入理由を分離した記述。不明な項目は推定しない。 |
| `## HTML直前の祖先` | HTMLへの採用との因果関係を確認できる先行物。なければ`未確認`。 |
| `## HTML外の由来` | 冒頭に`**到達度: 分類**`を一つ置く。`### 結論`に確認できた外部系譜または要求、接続が止まる位置を記し、`### 証拠`に史料が直接示す内容、`### 解釈`に証拠から導く説明を分ける。成立しない小節は`なし`とする。 |
| `## 系譜` | 確認済みの関係だけを結んだ系譜。集合レベルの関係と欠落を注記する。 |
| `## 証拠` | 本文の歴史的主張へ対応する証拠表。 |
| `## 確度` | 導入確度A、A−、B、C、未評価のいずれかと判定理由。 |
| `## 否定された仮説` | 撤回、反証、保留した説明と理由。なければ`なし`。 |
| `## 未解決` | 系譜上の欠落に対応する具体的な問い。なければ`なし`。 |

証拠表は次の列を持つ。

| 年月日 | 資料 | 種別 | この資料から確認できる内容 | 閲覧日 |
|---|---|---|---|---|

各節には、調査経緯ではなく現在の結論を直接記す。`外部候補あり`と`外部未確認`では、調べた範囲の列挙ではなく、確認済みの接続がどこで止まるかを`HTML外の由来`へ記し、再開条件を具体的な問いとして`未解決`へ記す。複数要素が共有する関係を共有系譜ページへ統合しても、要素ページにはその要素の結論を直接支える証拠を残す。

既存要素ページは移行が完了するまで`## さらに上流の由来`と現在の状態を維持できる。歴史部分を次に変更するとき、または明示的な移行操作で、新しい見出しとHTML外への到達度を追加する。概観の分類から機械的に転記せず、要素ごとに証拠と未解決点を確認する。全要素の移行後にこの経過措置を削除する。

### 共有系譜ページ

共有系譜ページは、共通祖先からの採用、複数要素を一体として導入した要求、または現行要素間の直接的な変更を一次資料で確認できる場合にのみ作る。同じ仕様に共存するだけの要素、単一要素だけで完結する系譜、機能上の対比にとどまる関係は収録しない。個別要素の初出、現在の意味、導入確度、HTML外への到達度、証拠表、未解決点は要素ページを正本とする。

### 概観ページ

概観ページは、要素ページと共有系譜を横断する分類、比較、調査状況の見取り図を扱う。個別の結論を複製せず、証拠と解釈を分け、粒度ごとの正本へリンクする。

## 操作

依頼に応じて次の操作を選ぶ。

| 依頼 | 操作 |
|---|---|
| 新しい資料を既存知識へ統合する | インジェスト |
| Wikiを使って質問へ回答する | クエリー |
| 未解決問題を一次資料まで遡って調べる | 調査 |
| 構造、参照、集計、状態を検査する | リント |
| このスキーマを変更する | スキーマ |

Wikiを書き換える操作では、影響する全ページと、ページ規約が求める場合はREADMEも同じ変更で整合させる。表現のみを修正する場合は、歴史的主張の意味、導入確度、HTML外への到達度、証拠との対応を変えない。LLMは指定された調査範囲内で資料と探索経路を選ぶ。対象範囲、解釈、強調点を変える重要な選択についてのみ、更新前に人間へ判断を求める。

更新後は、必要な敵対的検証、対象リント、状態の確定、ログの追記の順に実施して操作を完了する。歴史的結論、証拠、系譜、導入確度、HTML外への到達度を変更したページは、敵対的検証と対象リントに合格するまで`要検証`とする。

### Rawソースの保守

URLからRawスナップショットを追加するときは、正式な配布元または信頼できる保存元にある意図した文書であることを確認し、リポジトリルートで`node tools/add-raw-source.js <URL>`を実行する。ファイル種別の引数は指定しない。取得器は通常ファイル、ZIP、tarアーカイブを取得内容から自動判定し、通常ファイルはそのまま、アーカイブは安全性を検査してから展開して保存する。

大規模な資料群は、Wikiが参照する資料と、対象要素の集合または境界の検証に必要な資料だけを保存してよい。

追加に成功したら、標準出力にある取得元URL、リダイレクト後の最終URL、取得日時、保存先を該当操作のログへ記録する。Rawを変更した操作では、リポジトリルートで引数なしの`node tools/generate-raw-source-index.js`を実行する。

### 敵対的検証

敵対的検証は初回調査と分離し、結論を支持する探索の反復ではなく、反証を探す読み直しとして実施する。次のすべてを満たした場合だけ合格とする。

1. 各歴史的主張が知識モデルの証拠要件を満たす。
2. 日付、主体、要素名、引用の文脈を取り違えていない。
3. より早い版、競合資料、反例を探索し、結果を結論、否定された仮説、未解決へ反映している。
4. 系譜の各矢印、導入確度、HTML外への到達度、未解決点が証拠表と整合する。

由来を確定できない場合も、確認可能な最初期の出現、直接の証拠、未解決点を記録して検証を通過すれば正式な成果とする。

### インジェスト

1. 与えられた資料を識別し、未収録のURLは`tools/add-raw-source.js`で新しいRawスナップショットとして保存する。
2. Wiki内を検索して関連ページと既存Rawを読み、重複、補強、限定、矛盾、新しい接続を特定する。
3. 資料の要点を証拠、解釈、出発仮説へ分け、影響するページへ統合する。
4. 競合する説明を両方残し、結論、HTML外への到達度、否定された仮説、未解決を更新する。歴史的な変更があるページは`要検証`とする。
5. Raw索引を再生成し、対象リント、状態の確定、`ingest`ログの追記を行う。

### クエリー

1. Wiki内を検索して関連ページを選び、蓄積された結論を読む。
2. 必要な主張が未記録、競合、古い、または高い正確性を要する場合だけRawへ戻る。
3. WikiページとRawを引用して回答する。
4. 新しい主張、訂正、接続、再利用価値のある横断的な見取り図を得た場合にのみWikiへ書き戻す。必要ならHTML外への到達度も更新し、歴史的な変更があるページを`要検証`として、対象リント、状態の確定、`query`ログの追記を行う。

既存知識の言い換えや一度きりの回答はWikiへ保存せず、ログにも記録しない。

### 調査

1. Wiki内を検索し、関連する要素ページ、共有系譜ページ、概観ページ、未解決点を読んで、問いと既知の仮説を定める。
2. Living Standardで現在の定義を確認する。
3. HTML上の初出候補から以前の仕様、実装、DTD、議論へ遡り、導入主体、理由、直接祖先を探す。
4. 直接祖先が参照する上流の文書体系、ソフトウェア、メディア、UI慣習を調べ、特定の祖先がない場合は要素を生んだHTML外の要求を調べる。
5. 使用する新資料をRawへ追加し、証拠、解釈、仮説を分離してWikiへ統合する。
6. 初回探索と分離した敵対的検証で、反証、より早い出現、競合資料、別系統、引用の文脈欠落を探す。
7. 検証結果を結論、系譜、導入確度、HTML外への到達度、否定された仮説、未解決へ反映し、対象リント、状態の確定、`research`ログの追記を行う。

成果が得られなかった検索や作業経緯は正本ページへ残さない。調査中に追加した資料は、同じ操作内でインジェストの品質条件も満たす。別のログ項目には分けない。

### リント

対象リントでは、変更したページ、その直接の参照先と参照元、README、Raw索引、集計を検査する。全体リントを明示された場合は、全ページ、孤立ページ、矛盾の可能性、欠落した横断ページ、Raw参照、ログの整合性も検査する。

必須項目、見出し順、フロントマター、空の証拠表、汎用プレースホルダー、自然な日本語、保持すべき原表記、相対リンク、Raw参照、Raw索引の再現性とファイル網羅性、READMEのページ網羅性と概要の正確性、重複タイトル、状態、導入確度集計、HTML外への到達度集計を確認する。各項目を`合格|修正済み|Research必要`のいずれかとしてログに要約し、未分類の問題または安全に修正できる不合格が残る場合はリント未完了とする。結論を変えない機械的な不整合は修正する。歴史的主張、系譜、導入確度、HTML外への到達度、対象範囲に影響する問題は修正せず、`Research必要`として報告する。

明示的なリント操作では、対象、検査結果、修正内容、調査が必要な問題を`lint`のログ一項目へ記録する。

## ログ

`wiki/log.md`の本文は追記専用とし、過去の記録は書き換えない。ただし、正本文書の移動や削除に伴うリンク先の機械的な変更は例外とする。一つのユーザー起点の操作につき、一項目を記録する。

```text
## [YYYY-MM-DD] ingest|query|research|lint|schema | 対象
```

本文には統合した内容、変更した結論、検証結果、主要な制約を記す。過去の複数項目形式は履歴として維持する。
