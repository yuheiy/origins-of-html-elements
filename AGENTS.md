# The origins of HTML elements

このリポジトリは、現行HTML要素がどの文書体系、ソフトウェア、メディア、UI慣習、標準化活動から生まれたかを、史料に基づいて追跡するLLM Wikiである。現在の意味から起源を推測せず、確認できない接続は未解決のまま残す。

このファイルをWikiの唯一のSchemeとする。[llm-wiki.md](llm-wiki.md)は非規範的な設計背景であり、実際の構造と操作にはこのSchemeを適用する。

## Architecture

### Raw sources

`raw/`は、Wikiの主張が遡る不変のsource of truthである。LLMはIngestとResearchで資料を追加できるが、保存済みの内容を変更、上書き、削除しない。人間は資料と調査方向を選び、削除や訂正を明示的に承認する。

Wikiで根拠に使う資料は、現在仕様を含めて`raw/`へ保存する。外部URLは来歴と公開アクセスのために保持するが、固定された検証対象はローカルsnapshotとする。

### Wiki

`wiki/`はLLMが生成し、一貫性を保つ永続的な知識層である。人間は読み、問いと判断を与え、LLMはページの作成、統合、相互参照、訂正、Index、Logを管理する。

- `wiki/elements/`: 現行HTML要素ごとの結論。
- `wiki/sources/`: 複数ページから参照される、解釈に注意を要する、または文脈を再利用するRaw sourceの要約。Raw sourceごとの作成は必須ではない。
- `wiki/lineages/`: 複数の現行要素が共有する確認済みの上流、要求、標準化経路、または現行要素間の変更。
- `wiki/syntheses/`: 収録範囲、比較、横断分析、再利用する統合結果。対象集合の正本は`wiki/syntheses/scope.md`とする。
- `wiki/index.md`: 全Wikiページの生成Index。
- `wiki/log.md`: 操作単位のappend-onlyなLog。

### Scheme

`AGENTS.md`は構造、知識モデル、ページ契約、操作、完了条件を定義する唯一の規範である。Schemeの変更は人間の明示指示がある場合だけ行い、`schema`操作としてLogへ記録する。

## Knowledge model

### Evidence

史料は次の順に探索する。ただし、資料種別の順位より、主張への直接性、同時代性、来歴の明確さを優先する。

1. 当時の仕様、提案、DTD、議事録、メーリングリスト、実装資料、ソースコード。
2. 当事者による同時代または回顧的な説明。
3. 標準化団体や保存機関による史料解説。
4. 学術研究と出典を追跡できる二次資料。
5. 一般的な解説記事。

下位の資料は探索の入口に使えるが、到達可能な一次資料がある主張の最終根拠にはしない。複製を使う場合は元資料の作成者、日付、表題、保存元を記録する。

史料が矛盾するときは双方の主張、作成時期、立場、一次資料への距離を記録し、解消できない差異を`未解決`とする。後代の回顧は当事者の認識を示す証拠として扱い、同時代資料を単独で上書きする証拠にはしない。

### Claims

- **証拠**: 出典が明示する内容、または史料上直接観察できる内容。
- **解釈**: 複数の証拠から編集者が導く説明。証拠と同じ文へ圧縮しない。
- **出発仮説**: ハンドオフや既存研究から得たが、このWikiでは史料確認を終えていない説明。
- **否定された仮説**: 反証された説明、または由来として示すには証拠不足と判定した説明。
- **未解決**: 調査しても確定できない、追加調査で答えるべき具体的な問い。

歴史的な各主張には、それを直接支える出典を同じ段落または証拠表の同じ行に置く。証拠表ではRawへの相対リンクと外部URLを併記する。一つの参照を資料が述べていない主張へ流用せず、引用は判断に必要な短い範囲にとどめて前後関係を確認する。

因果関係は、当時の資料が採用、派生、改名、設計上の影響、または解決しようとした要求を示す場合だけ記す。名称、用途、外見の類似、現在の用途、後代の使用例は出発仮説にはなるが、単独で由来の証拠にはしない。未確認の接続を系譜の矢印で補わない。

出発仮説は結論や系譜へ確定事項として載せず、検証後に証拠、解釈、否定された仮説、または未解決へ移す。

### Confidence and status

確度はelementページだけに一つ置き、HTMLへの導入とその直前の系譜を評価する。調査量や現在の意味は評価しない。

| 等級 | 判定基準 |
|---|---|
| A | 要素単位の具体的な先行技術、要素、実装、慣習からHTMLへ入った因果関係を直接史料で確認できる。 |
| A− | 因果関係は直接確認できるが、集合単位の記述しかない、個別対応が不明、または中間経路の一部が欠ける。 |
| B | HTMLへの導入理由または設計モデルは確認できるが、具体的な先行物から当該要素への因果関係は確認できない。 |
| C | 確認可能な最初期の出現と当時の意味は特定できるが、導入理由と具体的祖先は確認できない。 |
| 未評価 | 調査範囲が不足し、AからCを判定できない。 |

全Wikiページはfrontmatterに`status: 調査中|要検証|完成`と一行の`summary`を持つ。`完成`はページ契約、知識検証、機械検査に合格した状態を表す。歴史的結論、証拠、系譜、確度を変更したページは`要検証`とし、敵対的検証後にだけ`完成`へ戻す。現在定義の確認日だけの変更は歴史部分の再検証を要しない。

`h1-h6.md`の確度と状態は`h1`から`h6`の6要素へそれぞれ計上する。

## Page contracts

### Element

ファイルは`wiki/elements/{要素名}.md`に置き、タイトルを``# `<element>` ``とする。`h1`から`h6`だけは`h1-h6.md`へまとめ、タイトルを``# `<h1>`〜`<h6>` ``とする。

次の見出しをこの順序で置く。

| 見出し | 必須内容 |
|---|---|
| `## 概要` | 判明した系譜と主要な未解決点の短い要約。 |
| `## 現在の意味` | Living Standard上の定義の要約、Rawと公開一次定義へのリンク、確認日。 |
| `## HTMLへの導入` | 初出、導入主体、導入理由を分離した記述。不明な項目は推定しない。 |
| `## HTML直前の祖先` | HTMLへの採用との因果関係を確認できる先行物。なければ`未確認`。 |
| `## さらに上流の由来` | `### 証拠`に史料が直接示す背景、`### 解釈`に証拠から導く説明。成立しなければ`なし`。 |
| `## 系譜` | 確認済みの関係だけを結んだ系譜。集合レベルの関係と欠落を注記する。 |
| `## 証拠` | 本文の歴史的主張へ対応する証拠表。 |
| `## 確度` | A、A−、B、C、未評価のいずれかと判定理由。 |
| `## 否定された仮説` | 撤回、反証、保留した説明と理由。なければ`なし`。 |
| `## 未解決` | 系譜上の欠落に対応する具体的な問い。なければ`なし`。 |

証拠表は次の列を持つ。

| 年月日 | 資料 | 種別 | この資料から確認できる内容 | Raw | 外部URL | 閲覧日 |
|---|---|---|---|---|---|---|

各節は調査経緯ではなく現在の結論を直接記す。複数要素が共有する関係をlineageへ統合しても、elementページにはその要素の結論を直接支える証拠を残す。

### Source

sourceページには原資料の識別情報、Rawへのリンク、外部URL、資料の射程、重要な記述、関連Wikiページ、解釈上の注意を含める。sourceページは原資料を置き換えず、elementページの証拠表を省略する理由にも使わない。見出し順は固定しない。

### Lineage

lineageページは、共通祖先からの採用、複数要素を一体として導入した要求、または現行要素間の直接変更を一次資料で確認できる場合だけ作る。同じ仕様に共存するだけの要素、単一要素だけで完結する系譜、機能上の対比だけの関係は収録しない。個別要素の初出、現在の意味、確度、証拠表、未解決点はelementページを正本とする。見出し順は固定しない。

### Synthesis

synthesisページは、収録範囲、比較、横断分析、Queryから得た再利用価値のある統合結果を扱う。証拠と解釈を分離し、粒度ごとの正本へリンクする。見出し順は固定しない。

同じ知識を複数ページへ全文再掲しない。sourceは資料が述べる内容、elementは要素単位の結論、lineageは複数要素の確認済み関係、synthesisは横断的な解釈を正本とする。

## Operations

Wikiを書き換える操作は、影響する全ページを同じ変更で整合させ、Indexを再生成し、検証後にLogへ一項目を追記して完了する。意味のある選択肢が対象範囲、解釈、強調点を変える場合だけ、更新前に人間へ判断を求める。

### Ingest

1. 与えられた資料を識別し、未収録なら新しいRaw snapshotとして保存してmanifestへ記録する。
2. Index、関連ページ、既存Rawを読み、重複、補強、限定、矛盾、新しい接続を特定する。
3. 資料の要点を証拠、解釈、出発仮説へ分け、影響するページへ統合する。sourceページは再利用条件を満たす場合だけ作る。
4. 競合する説明を両方残し、結論、否定された仮説、未解決、状態を更新する。
5. Index生成と対象Lintを実行し、`ingest`のLogを一項目追記する。

Rawの識別、影響する全ページへの統合、矛盾処理、Index、検証、Logが揃った時点を完了とする。

### Query

1. Indexから関連ページを選び、Wikiに蓄積された結論を読む。
2. 必要な主張が未記録、競合、古い、または高い正確性を要する場合だけRawへ戻る。
3. WikiページとRawを引用して回答する。
4. 新しい主張、訂正、接続、再利用価値のある統合結果を得た場合だけWikiへ書き戻し、Index、対象Lint、`query`のLogまで行う。

既存知識の言い換えや単発回答はWikiへ保存せず、Logにも記録しない。

### Research

1. Index、関連するlineageとsynthesis、未解決点を読み、問いと既知の仮説を定める。
2. Living Standardで現在の定義を確認する。
3. HTML上の初出候補から以前の仕様、実装、DTD、議論へ遡り、導入主体、理由、直接祖先を探す。
4. 直接祖先が参照する上流の文書体系、ソフトウェア、メディア、UI慣習を調べる。
5. 使用する新資料をRawへ追加し、証拠、解釈、仮説を分離してWikiへ統合する。
6. 初回探索と分離した敵対的検証で、反証、より早い出現、競合資料、別系統、引用の文脈欠落を探す。
7. 結論、系譜、確度、否定された仮説、未解決、状態を確定し、Index、対象Lint、`research`のLogまで行う。

成果がなかった検索や作業経緯は正本ページへ残さない。Research中に追加した資料は同じ操作内でIngestの品質条件も満たし、別のLog項目へ分割しない。

### Lint

通常のLintは変更ページ、参照先、Index、集計を検査する。全体Lintを明示された場合は全ページ、孤立ページ、矛盾候補、欠落した横断ページ、Raw参照、Log整合も検査する。

必須項目、見出し順、frontmatter、空の証拠表、汎用placeholder、相対リンク、Raw参照、重複タイトル、Indexの再現性、状態、確度集計を確認する。結論を変えない機械的な不整合は修正する。歴史的主張、系譜、確度、対象範囲へ影響する問題は修正せず、Researchが必要な問題として報告する。

修正した場合はIndexを再生成する。明示的なLint操作は、対象、検査結果、修正、Researchへ送る問題を`lint`のLog一項目へ記録する。

### Schema

Scheme変更は人間の明示指示に基づき、変更理由、文脈、trade-off、必要な移行を`schema`のLog一項目へ記録する。Scheme変更用Skillは置かない。

`.agents/skills/ingest`、`.agents/skills/query`、`.agents/skills/research`、`.agents/skills/lint`は対応する基本操作のshortcutであり、このSchemeだけを参照する。

## Index and Log

### Index

`wiki/index.md`は`node scripts/generate-index.mjs`が生成する完全生成物であり、手編集しない。Gitで管理し、Wikiを書き換えた操作と同じ変更へ含める。

生成器は`wiki/elements/`、`wiki/sources/`、`wiki/lineages/`、`wiki/syntheses/`を走査し、frontmatterの一行`summary`と`status`、タイトル、element本文の確度から、種別ごとの一覧、ページ状態、要素状態、要素確度を決定的な順序で作る。`h1-h6.md`は要素集計で6件として数える。未知のページ種別、必須frontmatterの欠落、不正な状態または確度、重複タイトル、壊れた内部リンクがあれば非ゼロ終了し、既存Indexを保持する。

収録範囲の説明はIndexへ手書きせず、`wiki/syntheses/scope.md`を更新する。Living Standardの対象集合が変わった場合は抽出規則、境界事例、確認日、関連ページ、Index、Logを同じ変更で更新する。

### Log

`wiki/log.md`はappend-onlyとし、過去の本文を書き換えない。正本文書の移動や削除に伴うリンク先だけは更新できる。項目は一つのユーザー起点の操作につき一つとする。

```text
## [YYYY-MM-DD] ingest|query|research|lint|schema | 対象
```

本文には統合した内容、変更した結論、検証結果、主要な制約を記す。過去の複数項目形式は履歴として維持する。

## Validation

敵対的検証は初回Researchと分離し、結論を支持する探索の反復ではなく、反証を探す読み直しとして実施する。次のすべてを満たした場合だけ合格とする。

1. 各歴史的主張をRawへ戻って確認でき、因果関係の主張に因果関係そのものの証拠がある。
2. 日付、主体、要素名、引用の文脈を取り違えていない。
3. より早い版、競合資料、反例を探索し、結果を結論、否定された仮説、未解決へ反映している。
4. 系譜の各矢印、確度、未解決点が証拠表と整合する。
5. ページ契約、相対リンク、Raw参照、Index、集計に不整合がない。

由来を確定できない場合も、確認可能な最初期の出現、直接の証拠、未解決点を記録して検証を通過すれば正式な成果とする。

## Raw source maintenance

Raw snapshotは追加後に不変とする。同じURLの内容が変わった場合は版または日付を識別できる別の保存先へ追加し、既存snapshotを保持する。誤収録の訂正や削除は人間の明示承認を得て、新しいLog項目から過去の記録を訂正する。

`raw/sources.tsv`には取得種別、保存先、取得元URL、取得日、内容識別子を記録する。`raw/README.md`には収録資料、版、目的、来歴を記録する。

HTMLとtextの応答本文は変換せず保存する。配布archiveは展開前に内部一覧を検査し、展開後の内容とディレクトリ構造を変更しない。archive自体を保存しない場合も取得元とdigestをmanifestへ残す。

取得は正式な配布元または信頼できる保存元から行い、意図した文書であることを確認する。取得処理は一時領域で完了してから新しい保存先へ追加し、失敗時に既存Rawを変更しない。
